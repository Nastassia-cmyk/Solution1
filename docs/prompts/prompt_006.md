Implement Admin Overview page:
- Show summary cards (total number of tasks in each status).
- Add a small table with last 5 completed tasks (a stub if there is no data yet).
- Keep layout simple, plain CSS.
- As data source - api/src/data/tasks.json

First output: plan + file list. Don't implement anything yet.

Don't create any *.md files with documentation.

You can only expand and update the existing README.md file so that it matches the latest changes made in the project.