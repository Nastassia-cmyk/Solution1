Add API endpoints: GET /api/src/admin/settings returns current { taskRepo }
POST /api/src/admin/settings saves { taskRepo } to api/src/data/settings.json (safe write)

Update /admin UI page: show current storage and a toggle/select to change it. 

Show orange message that restart is required.
First output: plan + file list. Don't implement yet.