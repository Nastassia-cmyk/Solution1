Implement Admin Users page:
- Show summary cards (total number of users in each role).
- Add a table with all the users (name and role), (a stub if there is no data yet).
- Keep layout simple, plain CSS.
- As data source - api/src/data/settings.json (users array)

Don't create any *.md files with documentation.

You can only expand and update the existing README.md file so that it matches the latest changes made in the project.